

function ChatMessageOnlyStyle({ message }) {
    return (
        <div className="message-wrapper-only">
            <div className="chat-box-message-only">{message.message}</div>
        </div>
    )
}

export default ChatMessageOnlyStyle